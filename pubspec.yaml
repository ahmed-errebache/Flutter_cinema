name: acteurs
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  custom_cached_image: ^1.7.0
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  dio: ^5.8.0
  shared_preferences: ^2.3.0
  flutter_svg: ^2.3.0
  flutter_map_marker_cluster: ^8.2.2


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/profile.jpg
    - assets/images/cinema.svg
